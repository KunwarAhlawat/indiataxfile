// Add your custom JS here.
// Show and Hide MegaMenu Item Functions Start
// Optionally, you can add a script to dynamically apply the class
document.addEventListener('DOMContentLoaded', function () {
    var paragraph = document.querySelector('.truncated-content');
    paragraph.classList.add('truncated-content');
});